# Index fresh-contributor pilot 01

Status: **READY — NOT EXECUTED**

This file prepares the first end-to-end fresh-contributor trial under [CONTRIBUTOR_CONFORMANCE.md](../../CONTRIBUTOR_CONFORMANCE.md). It is intentionally not marked PASS: the current maintainer/assistant session is not a qualifying blind run.

## Launch contract

**Entry URL:** `https://github.com/opensiro`

**Prompt — use verbatim:**

> Start at https://github.com/opensiro. Contribute one valid standalone assessment to the OpenSiro VSM Harness Index. Do not ask the maintainer for instructions unless you are genuinely blocked. Do not change unrelated files. Preserve repository source-of-truth boundaries and submit the work through the normal review path.

Fresh-run requirements:

- no OpenSiro project prompt;
- no prior OpenSiro conversation or memory supplied to the run;
- no preloaded `opensiro.com`, Organization, Index, Profile, Skills, batch, or candidate URL;
- public GitHub navigation allowed;
- repository write access allowed only for the normal contribution/PR path;
- no extra maintainer hints after launch unless a genuine blocker is recorded.

## Baseline observed while preparing the pilot

These values are **control evidence for later comparison, not launch hints**. The fresh agent must discover the then-current values itself.

- prepared: 2026-09-20;
- organization presentation entry exists at `opensiro/.github`;
- `opensiro.com` is linked as a non-normative orientation layer;
- `vsm-oss-organization/CONTRIBUTOR_START.md` is the shared contributor routing entry;
- `vsm-harness-index/AGENTS.md` is the Index machine-facing entry surface;
- Index sequential assessment guidance uses a Manual assessment board and `NEXT` row;
- Index exposes `scripts/assessment_preflight.py <issue-number>` for deterministic task-envelope assembly;
- the active semantic contract remains owned by `vsm-harness-index/data/active-contract.psv`;
- semantic work still requires current `vsm-harness-profile/main` and `vsm-harness-skills/main`.

Do not freeze these observations into the trial result. Record what the fresh run actually sees.

## Evidence form

### Run identity

| Field | Value |
| --- | --- |
| Batch | `index-pilot-01` |
| Run | `<run-id>` |
| Started at | `<timestamp>` |
| Fresh-run declaration | `<yes/no + how isolation was established>` |
| Entry URL | `https://github.com/opensiro` |
| Prompt | `verbatim launch prompt above` |

### Navigation and task acquisition

| Field | Value |
| --- | --- |
| Organization/profile revision observed | `<sha>` |
| Files/pages read in order | `<ordered list>` |
| Links followed in order | `<ordered list>` |
| Destination repository | `<repo>` |
| Assessment batch | `<issue>` |
| NEXT row | `<row + project>` |
| Frozen candidate repository | `<owner/repo>` |
| Frozen review ref | `<sha>` |
| Preflight command/result | `<command + captured output>` |

### Semantic contract

| Field | Value |
| --- | --- |
| Index active contract | `<Profile version / Methodology version>` |
| Profile main SHA read | `<sha>` |
| Skills main SHA read | `<sha>` |
| Function-first rule preserved | `<yes/no + evidence>` |
| Adjacent/external boundary preserved | `<yes/no + evidence>` |

### Delivery

| Field | Value |
| --- | --- |
| Files changed | `<paths>` |
| Structural assessment check | `<command + result>` |
| Generated-view checks | `<commands + results>` |
| Index final gate | `<command + result>` |
| PR | `<url>` |
| Review outcome | `<accepted / changes requested / rejected / blocked>` |
| Shared board advanced by agent | `<no unless explicitly authorized>` |

## Stage results

| Stage | Result | Evidence / failure |
| --- | --- | --- |
| Orientation | `PENDING` | |
| Routing | `PENDING` | |
| Task acquisition | `PENDING` | |
| Contract assembly | `PENDING` | |
| Semantic preparation | `PENDING` | |
| Bounded execution | `PENDING` | |
| Validation | `PENDING` | |
| Delivery | `PENDING` | |

## Final result

`READY — NOT EXECUTED`

When a qualifying run is performed, fill this record from that run's actual trace. If it fails or blocks, preserve the terminal record and create a new run file for another attempt rather than editing the failure into a pass.